{
  "name": "calorie-fitness-tracker",
  "version": "1.0.0",
  "description": "Calorie Fitness Tracker Android App",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step needed - static HTML app'",
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
